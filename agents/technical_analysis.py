"""Technical Analysis Agent for XAUUSD.\n\nวิเคราะห์แนวโน้ม Multi-timeframe, Indicators (EMA, RSI, ATR) และ Price Action\nสร้าง Signal คุณภาพสูงที่เน้น confluence และความชัดเจนของแนวโน้ม\n"""\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Optional\n\nimport pandas as pd\nimport numpy as np\n\nfrom core.indicators import ema, rsi, atr\n\n\n@dataclass\nclass Signal:\n    direction: str  # \"BUY\" | \"SELL\" | \"NONE\"\n    strength: float  # 0.0 - 1.0\n    entry_price: float\n    atr: float\n    reason: str\n    timeframe_bias: str = \"NEUTRAL\"\n    rsi_value: float = 50.0\n    ema_fast: float = 0.0\n    ema_slow: float = 0.0\n\n\nclass TechnicalAnalysisAgent:\n    \"\"\"วิเคราะห์ข้อมูลราคาและสร้าง Trading Signal คุณภาพสูงสำหรับ XAUUSD\"\"\"\n\n    def __init__(\n        self,\n        ema_fast: int = 20,\n        ema_slow: int = 50,\n        ema_trend: int = 200,\n        rsi_period: int = 14,\n        atr_period: int = 14,\n        rsi_oversold: float = 35.0,\n        rsi_overbought: float = 65.0,\n        pullback_atr_mult: float = 0.6,\n    ):\n        self.ema_fast = ema_fast\n        self.ema_slow = ema_slow\n        self.ema_trend = ema_trend\n        self.rsi_period = rsi_period\n        self.atr_period = atr_period\n        self.rsi_oversold = rsi_oversold\n        self.rsi_overbought = rsi_overbought\n        self.pullback_atr_mult = pullback_atr_mult\n\n    def analyze(\n        self,\n        df_m15: pd.DataFrame,\n        df_h1: pd.DataFrame,\n        df_h4: Optional[pd.DataFrame] = None,\n    ) -> Signal:\n        if df_m15 is None or len(df_m15) < max(self.ema_trend, 50):\n            return self._none_signal(\"ข้อมูล M15 ไม่เพียงพอ\")\n\n        if df_h1 is None or len(df_h1) < max(self.ema_trend, 50):\n            return self._none_signal(\"ข้อมูล H1 ไม่เพียงพอ\")\n\n        m15 = df_m15.copy()\n        h1 = df_h1.copy()\n\n        m15[\"ema_fast\"] = ema(m15[\"close\"], self.ema_fast)\n        m15[\"ema_slow\"] = ema(m15[\"close\"], self.ema_slow)\n        m15[\"rsi\"] = rsi(m15[\"close\"], self.rsi_period)\n        m15[\"atr\"] = atr(m15[\"high\"], m15[\"low\"], m15[\"close\"], self.atr_period)\n\n        h1[\"ema_fast\"] = ema(h1[\"close\"], self.ema_fast)\n        h1[\"ema_slow\"] = ema(h1[\"close\"], self.ema_slow)\n        h1[\"ema_trend\"] = ema(h1[\"close\"], self.ema_trend)\n        h1[\"rsi\"] = rsi(h1[\"close\"], self.rsi_period)\n\n        last_m15 = m15.iloc[-1]\n        prev_m15 = m15.iloc[-2]\n        last_h1 = h1.iloc[-1]\n\n        entry_price = float(last_m15[\"close\"])\n        current_atr = float(last_m15[\"atr\"]) if not pd.isna(last_m15[\"atr\"]) else 0.0\n        current_rsi = float(last_m15[\"rsi\"]) if not pd.isna(last_m15[\"rsi\"]) else 50.0\n\n        if current_atr <= 0:\n            return self._none_signal(\"ATR ไม่ถูกต้อง\")\n\n        h1_bias = self._get_h1_bias(last_h1)\n\n        buy_score, buy_reasons = self._score_buy(last_m15, prev_m15, last_h1, h1_bias, current_atr)\n        sell_score, sell_reasons = self._score_sell(last_m15, prev_m15, last_h1, h1_bias, current_atr)\n\n        if buy_score >= 0.55 and buy_score > sell_score and h1_bias in (\"BULLISH\", \"NEUTRAL\"):\n            return Signal(\n                direction=\"BUY\",\n                strength=round(min(buy_score, 1.0), 2),\n                entry_price=entry_price,\n                atr=round(current_atr, 2),\n                reason=\" | \".join(buy_reasons),\n                timeframe_bias=h1_bias,\n                rsi_value=round(current_rsi, 1),\n                ema_fast=float(last_m15[\"ema_fast\"]),\n                ema_slow=float(last_m15[\"ema_slow\"]),\n            )\n\n        if sell_score >= 0.55 and sell_score > buy_score and h1_bias in (\"BEARISH\", \"NEUTRAL\"):\n            return Signal(\n                direction=\"SELL\",\n                strength=round(min(sell_score, 1.0), 2),\n                entry_price=entry_price,\n                atr=round(current_atr, 2),\n                reason=\" | \".join(sell_reasons),\n                timeframe_bias=h1_bias,\n                rsi_value=round(current_rsi, 1),\n                ema_fast=float(last_m15[\"ema_fast\"]),\n                ema_slow=float(last_m15[\"ema_slow\"]),\n            )\n\n        return Signal(\n            direction=\"NONE\",\n            strength=0.0,\n            entry_price=entry_price,\n            atr=round(current_atr, 2),\n            reason=f\"ไม่มี confluence เพียงพอ (Buy={buy_score:.2f}, Sell={sell_score:.2f}, Bias={h1_bias})\",\n            timeframe_bias=h1_bias,\n            rsi_value=round(current_rsi, 1),\n            ema_fast=float(last_m15[\"ema_fast\"]),\n            ema_slow=float(last_m15[\"ema_slow\"]),\n        )\n\n    def _get_h1_bias(self, last_h1: pd.Series) -> str:\n        close = float(last_h1[\"close\"])\n        ema50 = float(last_h1[\"ema_slow\"])\n        ema200 = float(last_h1[\"ema_trend\"])\n\n        if pd.isna(ema50) or pd.isna(ema200):\n            return \"NEUTRAL\"\n\n        if close > ema50 and ema50 > ema200:\n            return \"BULLISH\"\n        if close < ema50 and ema50 < ema200:\n            return \"BEARISH\"\n        return \"NEUTRAL\"\n\n    def _score_buy(self, last, prev, last_h1, h1_bias, atr_val):\n        score = 0.0\n        reasons = []\n        close = float(last[\"close\"])\n        ema20 = float(last[\"ema_fast\"])\n        ema50 = float(last[\"ema_slow\"])\n        rsi_val = float(last[\"rsi\"])\n        prev_rsi = float(prev[\"rsi\"]) if not pd.isna(prev[\"rsi\"]) else rsi_val\n\n        if h1_bias == \"BULLISH\":\n            score += 0.35\n            reasons.append(\"H1 Bullish\")\n        elif h1_bias == \"NEUTRAL\" and close > ema50:\n            score += 0.15\n            reasons.append(\"H1 Neutral + above EMA50\")\n\n        if close > ema20:\n            score += 0.15\n            reasons.append(\"Close > EMA20\")\n\n        distance_to_ema20 = abs(close - ema20)\n        if distance_to_ema20 < atr_val * self.pullback_atr_mult and close > ema20:\n            score += 0.15\n            reasons.append(\"Pullback to EMA20\")\n\n        if prev_rsi < self.rsi_oversold and rsi_val > prev_rsi and rsi_val < 60:\n            score += 0.20\n            reasons.append(f\"RSI recovery ({prev_rsi:.0f}→{rsi_val:.0f})\")\n        elif 40 < rsi_val < 60:\n            score += 0.08\n            reasons.append(\"RSI mid-range\")\n\n        if ema20 > ema50:\n            score += 0.10\n            reasons.append(\"EMA20 > EMA50\")\n\n        return score, reasons\n\n    def _score_sell(self, last, prev, last_h1, h1_bias, atr_val):\n        score = 0.0\n        reasons = []\n        close = float(last[\"close\"])\n        ema20 = float(last[\"ema_fast\"])\n        ema50 = float(last[\"ema_slow\"])\n        rsi_val = float(last[\"rsi\"])\n        prev_rsi = float(prev[\"rsi\"]) if not pd.isna(prev[\"rsi\"]) else rsi_val\n\n        if h1_bias == \"BEARISH\":\n            score += 0.35\n            reasons.append(\"H1 Bearish\")\n        elif h1_bias == \"NEUTRAL\" and close < ema50:\n            score += 0.15\n            reasons.append(\"H1 Neutral + below EMA50\")\n\n        if close < ema20:\n            score += 0.15\n            reasons.append(\"Close < EMA20\")\n\n        distance_to_ema20 = abs(close - ema20)\n        if distance_to_ema20 < atr_val * self.pullback_atr_mult and close < ema20:\n            score += 0.15\n            reasons.append(\"Pullback to EMA20\")\n\n        if prev_rsi > self.rsi_overbought and rsi_val < prev_rsi and rsi_val > 40:\n            score += 0.20\n            reasons.append(f\"RSI rejection ({prev_rsi:.0f}→{rsi_val:.0f})\")\n        elif 40 < rsi_val < 60:\n            score += 0.08\n            reasons.append(\"RSI mid-range\")\n\n        if ema20 < ema50:\n            score += 0.10\n            reasons.append(\"EMA20 < EMA50\")\n\n        return score, reasons\n\n    def _none_signal(self, reason: str) -> Signal:\n        return Signal(\n            direction=\"NONE\",\n            strength=0.0,\n            entry_price=0.0,\n            atr=0.0,\n            reason=reason,\n            timeframe_bias=\"NEUTRAL\",\n        )\n\n    def prepare_dataframe(self, df: pd.DataFrame) -> pd.DataFrame:\n        out = df.copy()\n        out[\"ema_fast\"] = ema(out[\"close\"], self.ema_fast)\n        out[\"ema_slow\"] = ema(out[\"close\"], self.ema_slow)\n        out[\"ema_trend\"] = ema(out[\"close\"], self.ema_trend)\n        out[\"rsi\"] = rsi(out[\"close\"], self.rsi_period)\n        out[\"atr\"] = atr(out[\"high\"], out[\"low\"], out[\"close\"], self.atr_period)\n        return out\n
"""Simple but realistic Backtest Engine for XAUUSD Multi-Agent system.\n\nใช้ TechnicalAnalysisAgent + RiskManagementAgent\nรองรับ synthetic data และ real OHLCV\nสร้างกราฟ Equity + Price + Indicators\n"""\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\nfrom typing import Optional\nfrom pathlib import Path\nimport numpy as np\nimport pandas as pd\n\nfrom agents.technical_analysis import TechnicalAnalysisAgent, Signal\nfrom agents.risk_management import RiskManagementAgent, RiskDecision\n\n\n@dataclass\nclass Trade:\n    entry_time: pd.Timestamp\n    exit_time: Optional[pd.Timestamp]\n    direction: str\n    entry_price: float\n    exit_price: float\n    stop_loss: float\n    take_profit: float\n    lot_size: float\n    pnl: float\n    r_multiple: float\n    reason: str\n    strength: float\n\n\n@dataclass\nclass BacktestResult:\n    trades: list[Trade] = field(default_factory=list)\n    equity_curve: pd.Series = field(default_factory=pd.Series)\n    initial_equity: float = 10000.0\n    final_equity: float = 10000.0\n    total_return_pct: float = 0.0\n    max_drawdown_pct: float = 0.0\n    win_rate: float = 0.0\n    profit_factor: float = 0.0\n    total_trades: int = 0\n    avg_r: float = 0.0\n    sharpe_approx: float = 0.0\n\n\nclass BacktestEngine:\n    \"\"\"Event-driven style backtest (bar-by-bar) ที่ใช้ Agent จริงของระบบ\"\"\"\n\n    def __init__(\n        self,\n        ta_agent: Optional[TechnicalAnalysisAgent] = None,\n        risk_agent: Optional[RiskManagementAgent] = None,\n        initial_equity: float = 10000.0,\n        point: float = 0.01,\n        tick_value: float = 1.0,\n        commission_per_lot: float = 7.0,\n    ):\n        self.ta = ta_agent or TechnicalAnalysisAgent()\n        self.risk = risk_agent or RiskManagementAgent(\n            risk_per_trade=0.0075,\n            max_daily_drawdown=0.035,\n            max_open_positions=1,\n            min_reward_risk=1.6,\n            atr_sl_multiplier=1.8,\n            atr_tp_multiplier=3.0,\n        )\n        self.initial_equity = initial_equity\n        self.point = point\n        self.tick_value = tick_value\n        self.commission_per_lot = commission_per_lot\n\n    def run(self, df_m15: pd.DataFrame, df_h1: pd.DataFrame, lookback_h1: int = 300) -> BacktestResult:\n        m15 = df_m15.copy()\n        h1 = df_h1.copy()\n\n        if not isinstance(m15.index, pd.DatetimeIndex):\n            if \"time\" in m15.columns:\n                m15 = m15.set_index(\"time\")\n            m15.index = pd.to_datetime(m15.index)\n\n        if not isinstance(h1.index, pd.DatetimeIndex):\n            if \"time\" in h1.columns:\n                h1 = h1.set_index(\"time\")\n            h1.index = pd.to_datetime(h1.index)\n\n        m15 = m15.sort_index()\n        h1 = h1.sort_index()\n\n        equity = self.initial_equity\n        equity_curve = []\n        equity_times = []\n        trades: list[Trade] = []\n\n        open_trade: Optional[Trade] = None\n        daily_pnl = 0.0\n        current_day = None\n\n        start_idx = max(250, self.ta.ema_trend + 10)\n\n        for i in range(start_idx, len(m15)):\n            row = m15.iloc[i]\n            ts = m15.index[i]\n            day = ts.date()\n\n            if current_day is None or day != current_day:\n                daily_pnl = 0.0\n                current_day = day\n\n            equity_curve.append(equity)\n            equity_times.append(ts)\n\n            if open_trade is not None:\n                hit_sl = False\n                hit_tp = False\n                exit_price = float(row[\"close\"])\n\n                if open_trade.direction == \"BUY\":\n                    if float(row[\"low\"]) <= open_trade.stop_loss:\n                        hit_sl = True\n                        exit_price = open_trade.stop_loss\n                    elif float(row[\"high\"]) >= open_trade.take_profit:\n                        hit_tp = True\n                        exit_price = open_trade.take_profit\n                else:\n                    if float(row[\"high\"]) >= open_trade.stop_loss:\n                        hit_sl = True\n                        exit_price = open_trade.stop_loss\n                    elif float(row[\"low\"]) <= open_trade.take_profit:\n                        hit_tp = True\n                        exit_price = open_trade.take_profit\n\n                if hit_sl or hit_tp:\n                    pnl = self._calc_pnl(open_trade, exit_price)\n                    equity += pnl\n                    daily_pnl += pnl\n                    r_mult = self._calc_r(open_trade, exit_price)\n                    open_trade.exit_time = ts\n                    open_trade.exit_price = exit_price\n                    open_trade.pnl = round(pnl, 2)\n                    open_trade.r_multiple = round(r_mult, 2)\n                    trades.append(open_trade)\n                    open_trade = None\n                continue\n\n            m15_window = m15.iloc[: i + 1]\n            h1_window = h1[h1.index <= ts].tail(lookback_h1)\n\n            if len(h1_window) < 50:\n                continue\n\n            signal: Signal = self.ta.analyze(m15_window, h1_window)\n\n            if signal.direction == \"NONE\":\n                continue\n\n            decision: RiskDecision = self.risk.calculate(\n                equity=equity,\n                entry_price=signal.entry_price,\n                direction=signal.direction,\n                atr=signal.atr,\n                current_daily_pnl=daily_pnl,\n                open_positions=0 if open_trade is None else 1,\n                point=self.point,\n                tick_value=self.tick_value,\n            )\n\n            if not decision.approved:\n                continue\n\n            open_trade = Trade(\n                entry_time=ts,\n                exit_time=None,\n                direction=signal.direction,\n                entry_price=signal.entry_price,\n                exit_price=0.0,\n                stop_loss=decision.stop_loss,\n                take_profit=decision.take_profit,\n                lot_size=decision.lot_size,\n                pnl=0.0,\n                r_multiple=0.0,\n                reason=signal.reason,\n                strength=signal.strength,\n            )\n\n        if open_trade is not None:\n            last_price = float(m15.iloc[-1][\"close\"])\n            pnl = self._calc_pnl(open_trade, last_price)\n            equity += pnl\n            open_trade.exit_time = m15.index[-1]\n            open_trade.exit_price = last_price\n            open_trade.pnl = round(pnl, 2)\n            open_trade.r_multiple = round(self._calc_r(open_trade, last_price), 2)\n            trades.append(open_trade)\n\n        eq_series = pd.Series(equity_curve, index=pd.DatetimeIndex(equity_times))\n        return self._build_result(trades, eq_series, equity)\n\n    def _calc_pnl(self, trade: Trade, exit_price: float) -> float:\n        if trade.direction == \"BUY\":\n            points = (exit_price - trade.entry_price) / self.point\n        else:\n            points = (trade.entry_price - exit_price) / self.point\n        gross = points * self.tick_value * trade.lot_size\n        commission = self.commission_per_lot * trade.lot_size\n        return gross - commission\n\n    def _calc_r(self, trade: Trade, exit_price: float) -> float:\n        risk = abs(trade.entry_price - trade.stop_loss)\n        if risk <= 0:\n            return 0.0\n        if trade.direction == \"BUY\":\n            return (exit_price - trade.entry_price) / risk\n        return (trade.entry_price - exit_price) / risk\n\n    def _build_result(self, trades, equity_curve, final_equity) -> BacktestResult:\n        total_trades = len(trades)\n        if total_trades == 0:\n            return BacktestResult(trades=[], equity_curve=equity_curve, initial_equity=self.initial_equity, final_equity=final_equity)\n\n        wins = [t for t in trades if t.pnl > 0]\n        losses = [t for t in trades if t.pnl <= 0]\n        win_rate = len(wins) / total_trades * 100\n        gross_profit = sum(t.pnl for t in wins) if wins else 0.0\n        gross_loss = abs(sum(t.pnl for t in losses)) if losses else 1e-9\n        profit_factor = gross_profit / gross_loss if gross_loss > 0 else 0.0\n        avg_r = float(np.mean([t.r_multiple for t in trades]))\n\n        peak = equity_curve.cummax()\n        dd = (equity_curve - peak) / peak\n        max_dd = float(dd.min()) * 100\n\n        daily_eq = equity_curve.resample(\"1D\").last().dropna()\n        if len(daily_eq) > 2:\n            rets = daily_eq.pct_change().dropna()\n            sharpe = float(rets.mean() / (rets.std() + 1e-9) * np.sqrt(252))\n        else:\n            sharpe = 0.0\n\n        return BacktestResult(\n            trades=trades,\n            equity_curve=equity_curve,\n            initial_equity=self.initial_equity,\n            final_equity=final_equity,\n            total_return_pct=round((final_equity / self.initial_equity - 1) * 100, 2),\n            max_drawdown_pct=round(max_dd, 2),\n            win_rate=round(win_rate, 1),\n            profit_factor=round(profit_factor, 2),\n            total_trades=total_trades,\n            avg_r=round(avg_r, 2),\n            sharpe_approx=round(sharpe, 2),\n        )\n\n\ndef generate_synthetic_xauusd(n_bars: int = 3000, start_price: float = 2300.0, seed: int = 42, freq: str = \"15min\"):\n    np.random.seed(seed)\n    dates = pd.date_range(\"2024-01-01\", periods=n_bars, freq=freq)\n    returns = np.random.normal(0.00002, 0.0012, n_bars)\n    for i in range(0, n_bars, 400):\n        returns[i : i + 200] += 0.00015 * (1 if (i // 400) % 2 == 0 else -1)\n    close = start_price * np.exp(np.cumsum(returns))\n    high = close + np.abs(np.random.normal(0, 1.8, n_bars))\n    low = close - np.abs(np.random.normal(0, 1.8, n_bars))\n    open_ = np.roll(close, 1)\n    open_[0] = start_price\n    m15 = pd.DataFrame({\"open\": open_, \"high\": high, \"low\": low, \"close\": close}, index=dates)\n    h1 = m15.resample(\"1h\").agg({\"open\": \"first\", \"high\": \"max\", \"low\": \"min\", \"close\": \"last\"}).dropna()\n    return m15, h1\n